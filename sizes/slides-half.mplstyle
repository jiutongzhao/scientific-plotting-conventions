# Size patch — stack AFTER the base style (later entries override earlier ones):
#   plt.style.use(["slides.mplstyle", "sizes/slides-half.mplstyle"])
figure.figsize: 6.0, 4.5         # half of a 16:9 slide: two figures side by side

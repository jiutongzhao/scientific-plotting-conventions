# Size patch — stack AFTER the base style (later entries override earlier ones):
#   plt.style.use(["paper.mplstyle", "sizes/paper-onehalf.mplstyle"])
figure.figsize: 4.724, 2.92      # 120 mm "1.5-column" x golden ratio (publishers allow ~120-136 mm)
